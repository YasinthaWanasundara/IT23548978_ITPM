# Auto detect text files and perform LF normalization
* text=auto

# Ensure files with these extensions are treated as text
*.js text
*.json text
*.md text
*.txt text
*.yml text
*.yaml text
*.xml text
*.html text
*.css text
*.spec.js text

# Binary files - don't diff or merge
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.exe binary
*.dll binary

# Windows specific files
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Playwright specific
playwright-report/** -diff -merge
test-results/** -diff -merge
screenshots/** -diff -merge

# Node.js/NPM specific
package-lock.json -diff -merge
node_modules/** -diff -merge

# IDE/Editor files
.vscode/** -diff -merge
.idea/** -diff -merge
*.swp -diff -merge
*.swo -diff -merge

# OS generated files
.DS_Store -diff -merge
Thumbs.db -diff -merge

# Force LF line endings for these (cross-platform consistency)
*.js eol=lf
*.json eol=lf
*.md eol=lf
*.yml eol=lf
*.yaml eol=lf

# CRLF for Windows scripts
*.bat eol=crlf
*.cmd eol=crlf

# Don't diff or merge test result files
*.xml -diff -merge
*.trx -diff -merge